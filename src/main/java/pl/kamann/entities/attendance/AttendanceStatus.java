package pl.kamann.entities.attendance;

public enum AttendanceStatus {
    PRESENT,
    ABSENT,
    EARLY_CANCEL,
    LATE_CANCEL,
    REGISTERED,
    WAITLISTED,
    CANCELED_BY_CLIENT,
    CONFIRMED, CANCELED_BY_INSTRUCTOR
}
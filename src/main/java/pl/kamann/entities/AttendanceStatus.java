package pl.kamann.entities;

public enum AttendanceStatus {
    PRESENT,
    ABSENT,
    EARLY_CANCEL,
    LATE_CANCEL,
    REGISTERED,
    WAITLISTED,
    CANCELED_BY_CLIENT,
    CANCELED_BY_INSTRUCTOR
}